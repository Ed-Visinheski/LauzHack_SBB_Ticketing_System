{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LLDB: Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/bin/main",
      "args": ["--both"],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "preLaunchTask": "Build"
    }
  ]
}